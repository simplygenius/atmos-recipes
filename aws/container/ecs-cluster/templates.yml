description: Elastic Container Service
dependent_templates:
  - aws/scaffold
  - aws/vpc
  - aws/dns
  - aws/database
  - aws/load-balancer/application
  - aws/container/ecs
optional:
  # Always ignore since we template it through ERB to produce the recipe based
  # on the service name
  cluster_template.tf: "false"
